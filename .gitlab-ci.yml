stages:
    - ansible

.ansible__template: &ansible_template
    stage: ansible
    script:
        - ansible-playbook $PLAYBOOK
    when: manual
    
    
database_instance:
    <<: *ansible__template
    variables:
        PLAYBOOK: p2.yml
    only:
        changes:
            - p2.yml  
