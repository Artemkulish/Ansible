stages:
    - ansible

.ansible_template: &ansible_template
    stage: ansible
    script:
        - ansible-playbook $PLAYBOOK
    when: manual
    
    
database_instance:
    <<: *ansible_template
    variables:
        PLAYBOOK: p2.yml
    only:
        changes:
            - p2.yml  
